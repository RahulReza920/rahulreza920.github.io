---
import { CERTIFICATES } from '../lib/data';
---

<section id="certificates" class="prose">
	<h2>Certificates &amp; Achievements</h2>
	<ul class="plain stack" style="gap: 0.6rem;">
		{CERTIFICATES.map((c) => (
			<li>
				<a href={c.url}>{c.title}</a>
			</li>
		))}
	</ul>
</section>
